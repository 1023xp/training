#include <stdio.h>
#include <string.h>
int main(int argc, char *argv[])
{
	int i, len;

	printf("input len:");
	scanf("%d", &len);

	{
		char str[len];
		for (i = 0; i < len - 1; i++) {
			str[i] = 'a';
		}
		str[i] = 0;

		printf("%s\n", str);
	}
}
